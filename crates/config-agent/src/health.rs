// Health and diagnostics are available through:
// - `GET /v1/agent/health` — live health and metrics endpoint (when agent is running)
// - `config-agent --diagnose` — offline diagnostic checks (does not start the agent)